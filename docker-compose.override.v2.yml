version: '3'

services:
  web-v2:
    build:
      context: ./web
    image: webv3:latest
    env_file:
      - ./web/.env
    container_name: web-v3
    expose:
      - "4000"
    networks:
      - app-net
  app-v2:
    build:
      context: ./api
    image: appv3:latest
    env_file:
      - .env
    container_name: app-v3
    expose:
      - "3000"
    networks:
      - app-net

networks:
  app-net:
    external:
      name: ci-cd-nodejs-starter_app-net