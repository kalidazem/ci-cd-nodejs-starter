version: '3'

services:
  web-v5:
    build:
      context: ./web
    image: webv5:latest
    env_file:
      - ./web/.env
    container_name: web-v5
    expose:
      - "4000"
    networks:
      - app-net
  app-v5:
    build:
      context: ./api
    image: appv5:latest
    env_file:
      - .env
    container_name: app-v5
    expose:
      - "3000"
    networks:
      - app-net

networks:
  app-net:
    external:
      name: ci-cd-nodejs-starter_app-net